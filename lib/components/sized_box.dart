import 'package:flutter/material.dart';

class MySizedBox50 extends StatelessWidget {
  const MySizedBox50({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 50,
    );
  }
}

class MySizedBox25 extends StatelessWidget {
  const MySizedBox25({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 25,
    );
  }
}

class MySizedBox10 extends StatelessWidget {
  const MySizedBox10({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 10,
    );
  }
}
